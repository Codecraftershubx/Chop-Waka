import React from 'react';
import MainLayout from '../layouts/MainLayout.jsx';

export default function Home({ title, specials }) {
  return (
    <MainLayout title={title}>
      {/* ... existing JSX */}
    </MainLayout>
  )
}